import React, { Component } from 'react';

import './add.css'

export default class Add extends Component {
    render() {
        return (
            <button className="search-container__adding">+</button>
        )
    }
}